 int[] array = new int[10];
 Console.Write($"Napis 10 cisel:");
 for (int i = 0; i < array.Length; i++)
 {
     array[i] = int.Parse(Console.ReadLine());

 }

 int max = array.Max();
 int min = array.Min();

 int minmaxdifference = max - min;

 if (max < 0 && min < 0)
 {
     int lenthbetweenminmax = Math.Abs(max) - Math.Abs(min);
 } else if (max > 0 && min > 0){
     int lenthbetweenminmax = max - min;
 } else {
     int lenthbetweenminmax = max - -(min);
 }

using System;
class Program
{
    static bool ArrayPositiveNumbers(int[] array) 
    {
        Array.Sort(array);
        if (array[0] >= 0)
            return true;
        else return false;
    }

    /*   static bool ArraySort(Array array)
       {
           if (array = Array.Sort(array));
           return true;
           else return false;

       }*/

    static bool ArraySameNumbers(int[] array)
    {
        Array.Sort(array);
        for (int i = 1, num = array[0]; i < array.Length; i++) { 
        if (array[i] == num) return true;
        else num = array[i];
        } return false;
    }


    static bool ArraySameNumbers(int[] array)
    {




        static void Main()
    {
        int[] array = new int[5];
        Console.Write($"Napis 5 cisel:");
        for (int i = 0; i < array.Length; i++)
        {
            array[i] = int.Parse(Console.ReadLine());

        }


    }
}