#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
	srand(time(NULL));
	int arr[10];
	int prumer;

	for (int i = 0; i < 10; i++) {
		arr[i] = rand() % 6 + 1;
		printf("%d. znamka je %d", i, arr[i]);
	}
	for (int i = 0; i < 10; i++) {
		prumer = arr[] = arr[]

	}
return 0;
}