#include <stdio.h>
#include <string.h>

int main() {
    char word[101];
    
    printf("zadej lib slov:\n");
    scanf(" %100s", word);
    
    int len = strlen(word);
	
	for (int i = 0; i < len; i += 2) 
	    word[i] = '#';
	
	printf("%s",word);
	
	return 0;
}